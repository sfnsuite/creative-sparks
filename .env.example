# Public client configuration only. Never store service-role keys or database
# passwords here — those belong in Project Settings → Secrets and never in
# VITE_* variables or browser code.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=
